numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=1.7.0,<2.2        # shap 0.4x TreeExplainer cannot parse XGBoost >=2.2's base_score JSON format
catboost>=1.2.0
tensorflow>=2.12.0
shap>=0.42.0
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.10.0
joblib>=1.3.0
rasterio>=1.3.0
geopandas>=0.13.0
pyproj>=3.5.0
shapely>=2.0.0
statsmodels>=0.14.0
pingouin>=0.5.3
imbalanced-learn>=0.11.0
notebook>=7.0.0
tqdm>=4.65.0

# ---------------------------------------------------------------------------
# Windows / OneDrive note (see sibling project "Comparative Evaluation of
# Machine Learning" / Article C2 for the same issue): installing
# TensorFlow's wheel into a .venv INSIDE a long OneDrive path can fail with
# a Windows MAX_PATH error while unpacking its header tree. If that happens,
# create the venv outside OneDrive instead, e.g.:
#   python -m venv C:\Users\<you>\venvs\j1ml
#   C:\Users\<you>\venvs\j1ml\Scripts\pip install -r requirements.txt
#   C:\Users\<you>\venvs\j1ml\Scripts\python main.py
# ---------------------------------------------------------------------------
